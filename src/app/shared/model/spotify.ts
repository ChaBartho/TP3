export interface album {
  id: string;
  title: string;
  artist: string;
}

export interface artist {
  id: string;
  title: string;
}

export interface playlist {
  id: string;
  title: string;
  artist: string;
}

export interface player {
  id: string
}
